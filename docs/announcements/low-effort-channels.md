# Low-effort channels — one link, no social exposure

Each of these is a single pull request or form submission. No discussion,
no self-promotion — permanent discoverability.

## 1. This Week in Rust (newsletter, ~90k readers)

Submit a PR to <https://github.com/rust-lang/this-week-in-rust> adding one
line under **Project Updates** in the current draft (`draft/` folder):

```markdown
* [NeoEthos — a pure-Rust algorithmic trading engine with enforced anti-overfitting validation](https://github.com/kosred/Neoethos)
```

## 2. awesome-rust

PR to <https://github.com/rust-unofficial/awesome-rust>, section
**Applications → Finance** (or closest match), keeping their alphabetical
order and format:

```markdown
* [kosred/Neoethos](https://github.com/kosred/Neoethos) — Strategy-discovery and live-trading engine (genetic search, walk-forward/CPCV/PBO validation gates, native ML ensemble, Tauri desktop app). AGPL.
```

## 3. awesome-quant

PR to <https://github.com/wilsonfreitas/awesome-quant>, section **Rust**:

```markdown
- [NeoEthos](https://github.com/kosred/Neoethos) - Pure-Rust strategy discovery + live trading engine with enforced anti-overfitting validation (walk-forward, CPCV, PBO, permutation, plateau) and backtest↔live parity.
```

## 4. GitHub itself (already done, keep fresh)

- Repo description + topics: set (15 topics).
- The story page (<https://kosred.github.io/Neoethos/>) is linked from the
  repo homepage field.
- Each release with real release notes shows up in followers' feeds —
  releasing IS marketing.

## Notes

- Read each list's CONTRIBUTING first; match their entry format exactly —
  maintainers merge tidy PRs fast and silently.
- These compound slowly (search traffic, list browsers). Post-and-forget.
