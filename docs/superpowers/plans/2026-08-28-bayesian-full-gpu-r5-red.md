# Bayesian Logistic Full-GPU R5 RED Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Freeze an honest RED-only Bayesian CUDA acceptance bundle whose only compile failure is the missing lease-bearing CPU executor API and whose supporting math, evidence, AST, and provenance validators are independently executable.

**Architecture:** Keep production untouched. Separate locally GREEN support tests from the missing-API compile target and from behavior REDs, then make the ignored RTX parent consume the same wished-for CPU7 API, independent Laplace oracle, semantic CUPTI ledger, clean-tree dependency closure, and bounded serialized runner.

**Tech Stack:** Rust integration tests, `syn`/`quote` AST traversal, `nalgebra`/`ndarray` f64 reference math, `sha2`, `serde_json`, `rusqlite`, Cargo locked offline metadata, Git plumbing, NVIDIA Nsight Systems SQLite.

---

## Chunk 1: Reproducible authority and local validators

### Task 1: Track the exact local dependency closure

**Files:**
- Add: `vendor/catboost-rust-0.3.8-patched/**`
- Add: `vendor/cubecl-cuda-0.10.0-patched/**`
- Add: `vendor/cubecl-runtime-0.10.0-patched/**`
- Add: `vendor/cubek-convolution-0.2.0-patched/**`
- Add: `vendor/cubek-matmul-0.2.0-patched/**`
- Add: `vendor/lightgbm3/**`
- Add: `vendor/lightgbm3-sys/**`
- Add: `vendor/rlkit/**`
- Add: `vendor/sklears-core/**`
- Add: `vendor/vector-ta-0.2.9-patched/**`
- Add: `vendor/xgboost_lib-sys/**`
- Create: `audit/bayesian-full-gpu-r5-red/vendor-closure.sha256`

- [ ] **Step 1: Copy only the eleven restored authority vendor trees**

Copy from `C:\Users\konst\.codex\staging\vps-48599782-closeout-20260828\source\vendor`, excluding nested `target`, `.git`, and generated build outputs.

- [ ] **Step 2: Prove the workspace has no untracked dependency input**

Run: `git status --porcelain=v1 --untracked-files=all -- vendor`

Expected before commit: every required vendor file is listed for addition; no ignored-only dependency is used.

- [ ] **Step 3: Generate a deterministic per-file closure ledger**

For every `git ls-files --others --exclude-standard vendor` path, record lowercase SHA-256, byte length, and Git-relative path sorted ordinally. Reject symlinks/reparse points and files under `target`.

- [ ] **Step 4: Check locked offline metadata without building**

Run: `cargo +nightly-2026-04-07 metadata --locked --offline --no-deps --format-version=1`

Expected: exit 0 in the clean R5 clone with no external vendor copy.

- [ ] **Step 5: Commit the closure**

Run: `git add -f vendor audit/bayesian-full-gpu-r5-red/vendor-closure.sha256 && git commit -m "build(vendor): freeze Bayesian r5 offline closure"`

### Task 2: Build the independent Laplace oracle and semantic ledger validator

**Files:**
- Modify: `crates/neoethos-models/Cargo.toml`
- Create: `crates/neoethos-models/tests/bayesian_full_gpu_r5_support.rs`

- [ ] **Step 1: Write oracle tests before helpers**

Add three tests that demand independent posterior and probability results for normal, extreme-finite, and ill-conditioned fixtures. Each assertion covers all class weights, bias, full covariance shape/symmetry/positive diagonal, and non-empty normalized OOS probabilities.

- [ ] **Step 2: Run the support target and record the expected compile RED**

Run: `cargo +nightly-2026-04-07 test --locked --offline -p neoethos-models --no-default-features --test bayesian_full_gpu_r5_support --no-run`

Expected: compile failure because the test-owned oracle helpers do not exist yet.

- [ ] **Step 3: Implement the independent oracle**

Implement test-local deterministic scaling, chronological split, OVR MAP updates, full augmented Hessian, jittered Cholesky inverse, predictive variance correction, and softmax. Do not call `bayesian_impl` private helpers or deserialize expected values generated by production.

- [ ] **Step 4: Add semantic CUPTI tests**

Define normalized profiler records containing kernel name, start/end timestamps, grid work, and transfer direction/bytes. Require canonical Bayesian preprocessing, MAP-update, Hessian, Cholesky, and inference stages plus non-trivial duration and dimension-bound transfers. Add a negative test whose generic decoy kernel and CPU/fallback backend must be rejected with all missing stages listed.

- [ ] **Step 5: Add provenance tests**

Exercise deterministic file/byte hashing, sorted closure hashing, dirty-tree rejection, raw timing preservation, and dynamic commit/tree parsing with synthetic command outputs.

- [ ] **Step 6: Run the support target GREEN**

Run: `cargo +nightly-2026-04-07 test --locked --offline -p neoethos-models --no-default-features --test bayesian_full_gpu_r5_support -- --nocapture`

Expected: all support tests pass with `RUSTFLAGS=-Dwarnings`.

- [ ] **Step 7: Commit support tests**

Run: `git add crates/neoethos-models/Cargo.toml crates/neoethos-models/tests/bayesian_full_gpu_r5_support.rs Cargo.lock && git commit -m "test(models): add independent Bayesian r5 validators"`

## Chunk 2: Authentic RED surfaces

### Task 3: Freeze the exact CPU7 compile prerequisite

**Files:**
- Create: `crates/neoethos-models/tests/bayesian_full_gpu_r5_acceptance.rs`

- [ ] **Step 1: Write the wished-for API call first**

Acquire exactly seven permits from one `CpuPermitBroker`, transfer them to its `BudgetedCpuExecutor`, and call the wished-for lease-bearing method with a callback that receives `&CpuLease`.

- [ ] **Step 2: Pin the callback semantics**

Inside the callback assert delivered lease width seven, current private pool width seven, seven distinct native `neoethos-cpu-*` identities, and fresh acquisition rejection in every broadcast worker. Place the public CPU fit and predict calls in that same callback and pass its delivered lease directly.

- [ ] **Step 3: Verify the authentic compile RED**

Run: `cargo +nightly-2026-04-07 test --locked --offline -p neoethos-models --no-default-features --features statistical-gpu --test bayesian_full_gpu_r5_acceptance --no-run`

Expected: one Rust `E0599` family rooted only in the absent lease-bearing executor method; no syntax, type, fixture, vendor, or unrelated API errors.

### Task 4: Add public lifecycle and fail-all embargo behavior REDs

**Files:**
- Create: `crates/neoethos-models/tests/bayesian_full_gpu_r5_contract.rs`
- Create: `crates/neoethos-models/tests/training_summary_embargo_r5_contract.rs`

- [ ] **Step 1: Add the real public GPU lifecycle test**

Use a legal deterministic three-class fixture and call `BayesianLogitExpert` only through `ExpertModel::fit`, `predict_proba`, `save`, and `load`. Require exact non-empty repeatability, full artifact identity, corruption rejection, and transactional state preservation.

- [ ] **Step 2: Verify the lifecycle RED**

Run: `cargo +nightly-2026-04-07 test --locked --offline -p neoethos-models --no-default-features --features statistical-gpu --test bayesian_full_gpu_r5_contract -- --nocapture`

Expected: CPU control passes; GPU route fails at production `cpu_backend_for_policy` because no Bayesian CUDA lane exists.

- [ ] **Step 3: Write the recursive AST census**

Parse all 81 production Rust files. Resolve `use` renames, type aliases, local summary bindings, and constructor-function aliases. Trace the summary argument reaching each exact producer's `try_build_runtime_artifact_metadata` call, without relying on one spelling of `TrainingSummaryMetadata`.

- [ ] **Step 4: Accumulate all producer diagnostics**

Inspect Bayesian, linear, and deep producers independently, append each failure to a vector, and assert only after all three have been visited. The failure text must name all three files/functions and the missing distinct embargo flow.

- [ ] **Step 5: Verify the embargo RED**

Run: `cargo +nightly-2026-04-07 test --locked --offline -p neoethos-models --no-default-features --test training_summary_embargo_r5_contract -- --nocapture`

Expected: recursive parse support passes; the one fail-all producer test fails and its output contains Bayesian, linear, and deep diagnostics.

## Chunk 3: Bounded real-card parent and immutable freeze

### Task 5: Complete the ignored serialized acceptance parent

**Files:**
- Modify: `crates/neoethos-models/tests/bayesian_full_gpu_r5_acceptance.rs`

- [ ] **Step 1: Make the parent own fixtures and hashes**

Persist train-feature, train-label, OOS-feature, and OOS-label SHA-256 for each exact shape. Require worker-reported hashes to match independently generated parent expectations before timing.

- [ ] **Step 2: Use the oracle in both roles**

Save genuine public artifacts, parse posterior parameters into test-owned structures, and compare CPU/GPU posteriors and probabilities against each other and the independent oracle for normal/extreme/ill-conditioned gates before the large performance samples.

- [ ] **Step 3: Query named Nsight evidence**

Export profiler-owned SQLite, resolve CUPTI string IDs, persist raw kernel/API/memcpy records, and pass them through the already-GREEN semantic validator. Require fit stages and inference stage in their parent-owned capture ranges; generic activity is insufficient.

- [ ] **Step 4: Persist dynamic implementation identity**

Require `git status --porcelain=v1` empty. Record live `HEAD`, `HEAD^{tree}`, relevant source hashes, current executable hash, `Cargo.lock`, locked-offline metadata hash, and the tracked per-file vendor closure. Do not compare runtime HEAD to the preimplementation authority constant.

- [ ] **Step 5: Enforce the credit ceiling**

Use one create-new lock, one parent, serialized roles, one excluded warm-up and three raw timed samples, short command deadlines, a single 30-minute parent deadline, child kill-on-timeout, and natural stop-on-first assertion failure.

- [ ] **Step 6: Keep hardware execution ignored**

Mark only the parent and private worker entry tests ignored. Never execute them locally or without the explicit persistent evidence directory and acceptance sentinel.

### Task 6: Verify and freeze R5 evidence

**Files:**
- Create: `audit/bayesian-full-gpu-r5-red/README.md`
- Create: `audit/bayesian-full-gpu-r5-red/commands.txt`
- Create: `audit/bayesian-full-gpu-r5-red/*.log`
- Create: `audit/bayesian-full-gpu-r5-red/manifest.sha256`

- [ ] **Step 1: Format and prove the production boundary**

Run: `cargo +nightly-2026-04-07 fmt --all -- --check`

Run: `git diff 45c64cc -- crates/neoethos-models/src crates/neoethos-core/src crates/neoethos-execution-budget/src`

Expected: format exit 0 and empty production diff.

- [ ] **Step 2: Re-run every local GREEN/RED command fresh**

Set `CARGO_INCREMENTAL=0`, `RUSTFLAGS=-Dwarnings`, an isolated target, and capture complete stdout/stderr plus exit status. Use `--locked --offline` for every Cargo invocation.

- [ ] **Step 3: Validate classifications and exact counts**

Require all support tests GREEN; CPU7 compile only E0599; public lifecycle only the production GPU-policy RED; embargo output names all three producer failures; hardware tests remain unexecuted.

- [ ] **Step 4: Generate the immutable manifest**

Hash every changed file and evidence log except `manifest.sha256` itself, sorted by Git-relative path. Include base commit, current commit/tree placeholder, vendor count/bytes/aggregate, command exit codes, and production source diff count.

- [ ] **Step 5: Commit and re-verify**

Run: `git add ... && git commit -m "test(models): freeze Bayesian full-GPU r5 RED"`

Then record final `git rev-parse HEAD`, `git rev-parse HEAD^{tree}`, `git status --short`, and re-hash the exact committed bundle. Do not amend after reporting immutable identities.
