diff --git a/crates/forex-search/src/genetic/search_engine.rs b/crates/forex-search/src/genetic/search_engine.rs
--- a/crates/forex-search/src/genetic/search_engine.rs
+++ b/crates/forex-search/src/genetic/search_engine.rs
@@
 use super::evolution_math::{
     EvolutionSearchPolicy, ParentSelectionPolicy, SeenSignatureMemory, SurvivorSelectionPolicy,
-    apply_metrics, crossover, generate_random_genes, mutate, new_random_gene, select_parent_index,
-    select_survivor_indices, unique_candidate_or_retry,
+    apply_metrics, crossover, gene_signature_hash, generate_random_genes, mutate, new_random_gene,
+    select_parent_index, select_survivor_indices, unique_candidate_or_retry,
 };
@@
     let mut best_metrics = Vec::new();
     let mut profitable_archive: Vec<(Gene, [f64; 11], usize)> = Vec::new();
     let mut archive_seq = 0usize;
-    let mut seen_strategy_ids: HashSet<String> = HashSet::new();
+    let mut seen_archive_signatures: HashSet<u64> = HashSet::new();
@@
-            let sid = if gene.strategy_id.is_empty() {
-                format!(
-                    "{:?}|{:?}|{:.3}|{:.3}",
-                    gene.indices, gene.weights, gene.long_threshold, gene.short_threshold
-                )
-            } else {
-                gene.strategy_id.clone()
-            };
-            if !seen_strategy_ids.insert(sid) {
+            let archive_sig = gene_signature_hash(gene);
+            if !seen_archive_signatures.insert(archive_sig) {
                 continue;
             }
             profitable_archive.push((gene.clone(), *m, archive_seq));
diff --git a/crates/forex-search/src/cubecl_eval.rs b/crates/forex-search/src/cubecl_eval.rs
--- a/crates/forex-search/src/cubecl_eval.rs
+++ b/crates/forex-search/src/cubecl_eval.rs
@@
             } else {
-                let s = signals_flat[signal_base + i];
+                // Match the CPU evaluator: act on the prior bar signal and fill
+                // on the current bar. Using signal i here would reintroduce
+                // same-bar look-ahead in the full CUDA backtest path.
+                let s = signals_flat[signal_base + i - 1];
                 if s != 0 {
                     if !(max_trades_per_day > 0 && day_trade_count >= max_trades_per_day) {
                         in_pos = s;
