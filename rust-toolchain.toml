# Pinned to the nightly the operator builds with locally (rustc 1.96.0-nightly
# 2026-04-07). A fixed date keeps the toolchain identical across local hosts,
# rented NVIDIA machines, and CI without multiplying Cargo's adaptive job budget.
[toolchain]
channel = "nightly-2026-04-07"
